# Database migrations

Place ordered, reviewed schema migrations in this directory. Apply them through the migration process used by the deployment environment before starting a new application version.

Do not store production credentials or connection strings in migration files.
